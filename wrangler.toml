# Cloudflare Pages Configuration
# https://developers.cloudflare.com/pages/functions/wrangler-configuration/

name = "study-buddy-ai"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# Add non-sensitive build variables here if needed
NODE_VERSION = "20"

# Note for Secret Environment Variables:
# Add GEMINI_API_KEY securely via Cloudflare Dashboard:
# Workers & Pages -> your-project -> Settings -> Environment Variables -> Add "GEMINI_API_KEY" (Encrypt)
